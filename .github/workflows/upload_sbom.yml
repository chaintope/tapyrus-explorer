name: Upload SBOM

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Target branch to build (e.g. main or release/1.2)'
        required: true
        default: 'master'
  push:
    tags:
      - v*

jobs:
  upload_sbom_frontend:
    if: ${{ github.repository == 'chaintope/tapyrus-explorer' }}
    uses: chaintope/.github/.github/workflows/trivy-sbom.yml@main
    with:
      scan-target: "frontend/package-lock.json"
      ref: ${{ github.event.inputs.branch || github.ref_name }}
    secrets:
      dependencytrack-server: ${{ secrets.DEPENDENCYTRACK_SERVER }}
      dependencytrack-apikey: ${{ secrets.DEPENDENCYTRACK_APIKEY }}
      dependencytrack-project: ${{ secrets.DEPENDENCYTRACK_PROJECT_FRONTEND }}

  upload_sbom_backend:
    if: ${{ github.repository == 'chaintope/tapyrus-explorer' }}
    uses: chaintope/.github/.github/workflows/trivy-sbom.yml@main
    with:
      scan-target: "backend/package-lock.json"
      ref: ${{ github.event.inputs.branch || github.ref_name }}
    secrets:
      dependencytrack-server: ${{ secrets.DEPENDENCYTRACK_SERVER }}
      dependencytrack-apikey: ${{ secrets.DEPENDENCYTRACK_APIKEY }}
      dependencytrack-project: ${{ secrets.DEPENDENCYTRACK_PROJECT_BACKEND }}
